.navbar.navbar-default.navbar-fixed-top
  .container
    .navbar-header
      button.navbar-toggle(type='button', data-toggle='collapse', data-target='.navbar-collapse')
        span.sr-only Toggle navigation
        span.icon-bar
        span.icon-bar
        span.icon-bar
      //- a.navbar-brand(href='/')
      //-   span.ion-cube
      //-    Project name
      //- #aboutModal.modal.fade(tabindex="-1", role="dialog", aria-labelledby="aboutModalLabel", aria-hidden="true")
      //-     .modal-dialog
      //-       .modal-content
      //-         .modal-header
      //-           .close(type="button", data-dismiss="modal", aria-label="Close")
      //-             span(aria-hidden="true") &times
      //-         .modal-title
      //-           h4 About
      //-         .modal-body
      //-           p This is something about
      //-         .modal-footer
      //-           .btn.btn-default(type="button", data-dismiss="modal") Close
      #instructionsModal.modal.fade(tabindex="-1", role="dialog", aria-labelledby="instructionsModalLabel", aria-hidden="true")
          .modal-dialog
            .modal-content
              .modal-header
                .close(type="button", data-dismiss="modal", aria-label="Close")
                  span(aria-hidden="true") &times
              .modal-title.text-center
                h4 How It Works
              .modal-body
                ul
                  li Enter a URL for a YouTube video and click the "Click to Get Video" button
                  li The YouTube video is played with various shortcuts at the top to make captioning easier
                  li If the YouTube video has Automatic Speech Recognition captions then they will display in the text boxes. Or if there are no captions then blank boxes will be displayed
                  li With either option, the text and timing of the captions can be edited and saved using the CTRL + Shift + S (Save Current Caption) shortcut
              .modal-footer
                .btn.btn-default(type="button", data-dismiss="modal") Close
    .collapse.navbar-collapse
      ul.nav.navbar-nav
        li(class=title=='Home'?'active':undefined)
          a(href='/') Home
        //- li(class=title=='About'?'active':undefined)
        //-   a(href='#', data-toggle="modal" data-target="#aboutModal") About
        li(class=title=='How It Works'?'active':undefined)
          a(href='#', data-toggle="modal" data-target="#instructionsModal") How It Works
        //- li(class=title=='API Examples'?'active':undefined)
        //-   a(href='/api/stripe') Donate
        //- li(class=title=='Contact'?'active':undefined)
        //-   a(href='/contact') Contact
      ul.nav.navbar-nav.navbar-right
        if !user
          li(class=title=='Login'?'active':undefined)
            a(href='/login') Login/Sign Up
          //- li(class=title=='Create Account'?'active':undefined)
          //-   a(href='/signup') Create Account
        else
          li.dropdown(class=title=='Account Management'?'active':undefined)
            a.dropdown-toggle(href='#', data-toggle='dropdown')
              if user.profile.picture
                img(src='#{user.profile.picture}')
              else
                img(src='#{user.gravatar(60)}')
              | #{user.profile.name || user.email || user.id}&nbsp;
              i.caret
            ul.dropdown-menu
              li
                a(href='/account')
                  span.ion-person
                  | My Account
              li.divider
              li
                a(href='/logout')
                  span.ion-log-out
                  |  Logout